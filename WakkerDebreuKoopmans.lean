/-
Wakker (1989) IV.2.7 and Debreu--Koopmans (1982) split barrel.

The former monolithic `WakkerDebreuKoopmans.lean` is now decomposed into
reviewer-sized modules under `WakkerDebreuKoopmans/` while preserving this
stable import path for existing files.
-/

import WakkerDebreuKoopmans.Core
import WakkerDebreuKoopmans.Certificates
import WakkerDebreuKoopmans.M2Frontier
import WakkerDebreuKoopmans.ConstructionStack
import WakkerDebreuKoopmans.Topology
import WakkerDebreuKoopmans.Closure
import WakkerDebreuKoopmans.Audit
